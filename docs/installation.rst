============
Installation
============

At the command line::

    $ easy_install prestoadmin

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv prestoadmin
    $ pip install prestoadmin
